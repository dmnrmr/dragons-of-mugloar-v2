version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10.15.3
    steps:
      - checkout
      - run:
          name: dependency-install
          command: npm ci
      - run:
          name: lint
          command: npm run lint
      - run:
          name: unit-tests
          command: npm t
